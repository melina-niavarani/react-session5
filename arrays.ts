interface Person {
    name: string;
    lastName: string;
    age: number;
    is_present: boolean;
}

const values: number[]= [];
const names: string[]= []
const contact: Person[]= []; 

values.push(10)
values.push("mel")

names.push("melina")
names.push(20)

contact.push({
    name: "foolan",
    lastName: "bisar",
    age: 10,
    is_present: false,
})